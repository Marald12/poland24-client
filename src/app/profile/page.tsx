import React from 'react'
import { NextPage } from 'next'
import ProfileSettings from '@/widgets/profile-page/settings/ProfileSettings'

const ProfilePage: NextPage = () => {
	return <ProfileSettings />
}

export default ProfilePage
