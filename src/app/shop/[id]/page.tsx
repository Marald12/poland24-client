import { NextPage } from 'next'
import SingleShopProvider from '@/widgets/single-shop-page/provider/SingleShopProvider'

const singleShopPage: NextPage = () => {
	return <SingleShopProvider />
}

export default singleShopPage
